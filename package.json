{
  "name": "simple-host-app",
  "version": "0.0.1",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Alert: Starting Development\" && electron .",
    "build": "echo `Alert: Starting Build` && electron-builder --config=build.js --publish=always"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^18.0.4",
    "electron-builder": "^23.0.3",
    "electron-forge": "^5.2.4",
    "electron-updater": "^5.0.1"
  }
}
